apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: alert-rules-adguard
    files:
      - rules.yaml
    options:
      labels:
        grafana_alert: "1"
