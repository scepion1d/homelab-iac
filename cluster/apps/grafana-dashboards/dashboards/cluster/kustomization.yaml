apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: dashboard-cluster
    files:
      - cluster.json=dashboard.json
    options:
      labels:
        grafana_dashboard: "1"
