apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: dashboard-dns
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: "DNS"
    files:
      - dashboard.json
